# 受注不可日マスタ

- 略号: `BD`
- 画面: `src/views/BlockedDateListView.vue`
- テスト: `e2e/blocked-dates.spec.js`

`/masters/blocked-dates` の受け入れ条件。ページ位置と検索条件は URL クエリ（`offset` / `date_from` /
`date_to`）を正としているため、**実ブラウザでの URL と画面の同期**をここで守る。
既定モックは 2023〜2029 年の 56 件（1 ページ 50 件）。

一覧・検索（BD-01〜10）に加えて、ヘッダの「新規追加」からの登録（BD-11〜18）と
行ごとの削除（BD-19〜23）もこの画面の受け入れ条件に含む。行の操作は削除だけなので、
BD-08 では表の列順（`日付` / `対象市場` / `理由` / 操作）と、操作列が右端にあることを守る。
[海外休場日マスタ](market-holidays.md)にある休場区分は持たない。

削除の既定モックも追加と同じく状態を保持するため、確認モーダルを経て
**削除後に件数が 1 減る**ところと、最終ページの最後の 1 件を消したときに
1 ページ前へ戻る URL の追従まで実ブラウザで見る。

登録は「サーバの事前検証 → 登録」の 2 段で、エラーの出し先が 3 系統に分かれる
（必須未入力は項目の直下 / 事前検証の不合格は箇条書き / 通信・サーバ障害は専用の表示）。
どの系統がどこに出るかを BD-12 / 14 / 16 で守る。登録の既定モックは追加した行を保持するため、
**登録後に件数が 1 増える**ところまで実ブラウザで見る（モックの状態はページを開くたびに初期化される）。

画面上部の説明バナー（国内拠点の受注停止日を管理する旨）は 4 状態の外にあり、
エラーでも 0 件でも消えない。検索カードも同じ理由で消えない（0 件やエラーのときこそ条件を直したい）。
`?offset=7` のような端数の扱いと、送信中に「追加中…」「削除中…」となってボタンが押せなくなる
過渡状態（応答を遅らせる必要があり実ブラウザでは安定して観測できない）、および
画面の導線からは起こらない削除の 404（未知 id）は
[docs/unit/views-blocked-date-list-view.md](../unit/views-blocked-date-list-view.md)
側の担保に譲り、ここには書かない。

| ID | 前提 | 操作 | 期待結果 | 状態 |
|---|---|---|---|---|
| BD-01 | 既定モック | `/` からサイドメニューの「受注不可日マスタ」を click | URL が `/masters/blocked-dates` になり、ヘッダに見出し「受注不可日マスタ」と「再読み込み」ボタンが表示される。件数「56 件」、表に 50 行あり、1 行目に 2023-01-01 / NYSE/NASDAQ / 元日（市場休場） が表示される | 実装済 |
| BD-02 | 既定モック（56 件） | 一覧を開いて「次のページ」を押す | URL に `offset=50` が付き、表が 6 行になる。1 行目に 2029-05-04 相当の 2 ページ目先頭の日付と理由、ページ送りに「56 件中 51–56 件」が表示される | 実装済 |
| BD-03 | 既定モック | 日付（From）に 2025-01-01、日付（To）に 2025-12-31 を入れて「検索」を押す | URL に `date_from` と `date_to` が付き、件数「8 件」・表 8 行になり、すべての行の日付が 2025 年になる | 実装済 |
| BD-04 | BD-03 で絞り込んだ状態 | 「クリア」を押す | URL から検索条件が消え、件数が「56 件」に戻り、日付の入力欄が空になる | 実装済 |
| BD-05 | API が 500 を返す | 一覧を開く | エラーメッセージ（サーバーでエラーが発生しました。）と「再試行」ボタンが表示され、表は表示されない。検索フォームは表示されたまま | 実装済 |
| BD-06 | 受注不可日が 0 件 | 一覧を開く | 「該当する受注不可日はありません。」が表示され、行は無い。件数は「0 件」 | 実装済 |
| BD-07 | 既定モック | `/masters/blocked-dates?offset=50` を直接開く | 2 ページ目の状態で表示される（表 6 行、ページ送りの「2」が現在ページ） | 実装済 |
| BD-08 | 既定モック | 一覧を開く | 表の見出しが左から `日付` / `対象市場` / `理由` / （見出し無し）の順に並び、各行の右端に「削除」ボタンがある。理由の列は削除ボタンより左にある。追加はヘッダの「新規追加」ボタンから行う | 実装済 |
| BD-09 | 既定モックと 0 件の両方 | 一覧を開く | 画面上部の説明バナーが、データがあるときも 0 件のときも表示される | 実装済 |
| BD-10 | BD-03 で絞り込んだ状態 | ヘッダの「再読み込み」を押す | URL の検索条件は変わらず、件数「8 件」・表 8 行のまま。入力欄の値も保たれる | 実装済 |
| BD-11 | 既定モック | ヘッダの「新規追加」を押す | 「受注不可日 新規追加」モーダルが開き、日付と理由の入力欄が空で表示される。入力項目はこの 2 つだけ | 実装済 |
| BD-12 | 追加モーダルを開いた状態 | 未入力のまま「追加」を押す | 「日付を入力してください。」「理由を入力してください。」が項目ごとに表示され、モーダルは閉じない。事前検証の箇条書きも通信エラーの表示も出ず、件数は「56 件」のまま | 実装済 |
| BD-13 | 既定モック | 一覧に無い日付（2030-01-01）と理由を入れて「追加」を押す | モーダルが閉じ、成功メッセージ（2030-01-01 を追加しました。）が表示され、件数が「57 件」になる。URL は `/masters/blocked-dates` のまま変わらない | 実装済 |
| BD-14 | 既定モック | 既にある日付（2023-01-01）と理由を入れて「追加」を押す | モーダルは閉じず、事前検証の理由の箇条書きに「その日付の受注不可日はすでに登録されています。」が表示される。項目直下のエラーには混ざらず、件数は「56 件」のまま | 実装済 |
| BD-15 | 事前検証が理由を 2 件返す | 一覧に無い日付と理由を入れて「追加」を押す | 箇条書きが 2 行になり、返された 2 つの文言がそれぞれ表示される。モーダルは閉じない | 実装済 |
| BD-16 | 登録 API が 500 を返す（事前検証は通る） | 一覧に無い日付と理由を入れて「追加」を押す | モーダルは閉じず「サーバーでエラーが発生しました。」が表示される。事前検証の箇条書きは出ず、成功メッセージも出ない。件数は「56 件」のまま | 実装済 |
| BD-17 | BD-14 で事前検証に弾かれた状態 | 「キャンセル」で閉じ、もう一度「新規追加」を押す | 箇条書きのエラーが消え、日付と理由の入力欄が空に戻っている | 実装済 |
| BD-18 | 追加モーダルを開いた状態 | 理由に 46 文字を入力する | 理由の入力欄には 45 文字しか入らない | 実装済 |
| BD-19 | 既定モック | 一覧 1 行目の「削除」を押す | 「削除確認」モーダルが開き、その行の日付（2023-01-01）と「を削除しますか？」「この操作は元に戻せません。」が表示される | 実装済 |
| BD-20 | BD-19 でモーダルを開いた状態 | 「キャンセル」を押す | モーダルが閉じ、件数「56 件」・表 50 行のまま変わらない。成功メッセージも出ない | 実装済 |
| BD-21 | 既定モック | 1 行目（2023-01-01）の「削除」→「削除する」を押す | モーダルが閉じ、成功メッセージ（2023-01-01 を削除しました。）が表示される。件数が「55 件」になり、2023-01-01 の行が消える。URL は `/masters/blocked-dates` のまま変わらない | 実装済 |
| BD-22 | 削除 API が 500 を返す | 1 行目の「削除」→「削除する」を押す | モーダルは開いたままエラー（サーバーでエラーが発生しました。）が表示され、件数は「56 件」のまま。行も消えず、成功メッセージも出ない | 実装済 |
| BD-23 | 既定モック。2023-09-07 以降で絞り込んだ 51 件の 2 ページ目（`?offset=50`、1 件） | その 1 件の「削除」→「削除する」を押す | 1 ページ目に戻り URL から offset が消える（絞り込み条件は残る）。表は 50 行、件数は「50 件」 | 実装済 |
