# 顧客マスタ

- 略号: `CU`
- 画面: `src/views/CustomerListView.vue`
- テスト: `e2e/customers.spec.js`

**当てる先は MSW のモック。** 期待値にフィクスチャの中身（56 件・特定の口座番号・特定の金額）を
使うので、実 API に当てると通らない。実 API との噛み合わせを見る版はまだ無い。

`/masters/customers` の受け入れ条件。ページ位置と検索条件は URL クエリ（`offset` /
`branch_code` / `handler_code` / `account_number` / `customer_name` / `restriction` /
`account_type` / `corporate_type`）を正としているため、**実ブラウザでの URL と画面の同期**
（CU-14）と**ブラウザバック**（CU-15）をここで守る。

既定モックは部店 4 × 顧客 14 の 56 件（1 ページ 50 件）で、実 API と同じ**口座番号の昇順**。
モックはこのほかに削除済み（論理削除）の行を 1 件持つが、既定の一覧には出ない。

この画面は**いま読むだけ**で、新規追加・編集・削除の導線を持たない（CU-11）。
導線が入ったらここにシナリオを足す。

**米国株評価額 / 評価損益は見出しだけで値を持たない。** 対応する項目が実 API に無く値の出所が
未定のため、列（見出し）だけ確保してセルは常に `—` にしてある。これは仕様なので、
「どの行も `—`」を CU-11 で守る（値が入り始めたらこの行を書き換える）。

**ユーザー操作フラグが 1 の行には色が付く。** 自動取込のままの行と見分けるための印なので、
「色が付いている行がある」ことではなく「フラグ 1 の行だけ背景色が他と違う」ことを守る（CU-10）。

**扱者コードの絞り込みはサーバ側に無い。** 2026-09-15 の OpenAPI 取り込みでこのマスタ一覧は
`/customers` から `/masters/customers` へ移り、そこに `handler_code` が無い。取引規制・口座区分・
個人法人と同じく **MSW のモックだけがこの条件を解釈する**ので、実 API に当てると扱者では絞られない。
ここは MSW に当てる文書なので、下のシナリオの期待値は変わらない。

検索の 7 項目のうちプルダウン 5 つは起動時に 1 回だけ読む `GET /codes` 由来なので、
**選択肢が埋まっていること自体がコードマスタの読み込みが効いている証拠**になる（CU-05）。
顧客名の検索は顧客名・顧客名カナへの部分一致だが、フィクスチャのカナは半角なので
全角で入れてもカナには当たらない。ここでは漢字での部分一致（CU-03）と
他条件との併用（CU-04）だけを見る。

`mockApi()` は固定の body を返すだけで `offset` やクエリを解釈しないため、
ページングと絞り込み（CU-02 / 03 / 04 / 06 / 07 / 08）はクエリを実際に処理する既定ハンドラで見る。

初期表示のローディングはモック応答が即座に返るため実ブラウザでは安定して観測できない。
ローディング表示の出し分け、`?offset=7` のような端数の `offset`、未知のコードを手で URL に
書いたときの扱いは画面の導線からは起こらないので、`docs/unit/` の
`CustomerListView` 側の担保に譲り、ここには書かない。

| ID | 前提 | 操作 | 期待結果 | 状態 |
|---|---|---|---|---|
| CU-01 | 既定モック | `/` からサイドメニューの「顧客マスタ」を click | URL が `/masters/customers` になり、ヘッダに見出し「顧客マスタ」が表示される。件数「56 件」、表に 50 行あり、1 行目にフィクスチャ先頭（口座番号の最小）の口座番号・顧客名・部店・扱者が表示される | 実装済 |
| CU-02 | 既定モック（56 件） | 一覧を開いて「次のページ」を押す | URL に `offset=50` が付き、表が 6 行になる。1 行目は 51 番目の口座番号・顧客名。ページ送りに「56 件中 51–56 件」が表示される | 実装済 |
| CU-03 | 既定モック | 顧客名に `山田` を入れて「検索」を押す | URL に `customer_name=山田` が付き、件数「4 件」。表の全行の顧客名に `山田` が含まれる | 実装済 |
| CU-04 | 既定モック | 部店コードで 2 番目の部店を選び、顧客名に `山田` を入れて「検索」を押す | URL に `branch_code` と `customer_name` の両方が付き、件数「1 件」。その行の部店コードが選んだ部店になる | 実装済 |
| CU-05 | 既定モック | 一覧を開いて検索カードのプルダウンを見る | 部店コード / 扱者コード / 取引規制 / 口座区分 / 個人／法人 の 5 つに、コードマスタの選択肢が先頭の「すべて」相当と合わせて並んでいる | 実装済 |
| CU-06 | 既定モック | 「個人／法人」で「法人」を選んで「検索」を押す | URL に `corporate_type=1` が付き、件数「8 件」。表の全行の個人／法人が「法人」になる | 実装済 |
| CU-07 | 既定モック。顧客名で絞り込み済み | 「クリア」を押す | URL のクエリが消え、件数が 56 件に戻り、顧客名の入力欄が空になる | 実装済 |
| CU-08 | 既定モック | 顧客名に該当しない文字列を入れて「検索」を押す | 「該当する顧客はありません。」が表示され、表は出ない。検索カードは残る | 実装済 |
| CU-09 | `GET /masters/customers` が 500 を返す | 一覧を開く | エラーの理由と「再試行」ボタンが表示され、表は出ない。説明バナーと検索カードは残る | 実装済 |
| CU-10 | 既定モック | 一覧を開いて行の背景色を比べる | ユーザー操作フラグ 1 の行だけ背景色がフラグ 0 の行と異なる | 実装済 |
| CU-11 | 既定モック | 一覧を開く | 列が `部店` / `扱者` / `口座番号` / `顧客名` / `年齢` / `取引規制` / `投資方針` / `コンプラ` / `口座区分` / `個人／法人` / `円貨預り金` / `USD預り金` / `成長投資枠` / `米国株評価額` / `評価損益` の順に並ぶ。米国株評価額と評価損益はどの行も `—`。ヘッダに「新規追加」は無く、行に編集・削除のボタンも無い | 実装済 |
| CU-12 | 既定モック | 一覧を開いて金額と年齢の表記を見る | 金額は通貨記号ではなく単位を後置（`3,500,000 円` / `50,000.00 ドル`）。値が無い行は `—`、残高 0 の行は `0 円` で `—` と区別される。年齢は `75歳` の形で、法人の行は `—` | 実装済 |
| CU-13 | 既定モック | 一覧を開いてバッジを見る | 取引停止の行の取引規制列にバッジが出て、通常の行には出ない。事故処理口座の行は口座区分にバッジ「事故」が併記され、通常の口座には出ない | 実装済 |
| CU-14 | 既定モック | 部店コードと顧客名のクエリを付けた URL を直接開く | 検索欄にそのクエリの値が復元され、一覧がその条件で絞り込まれた状態で表示される | 実装済 |
| CU-15 | 既定モック。2 ページ目を開いている | ブラウザの戻るを押す | URL から `offset` が消え、表が 1 ページ目に戻る | 実装済 |
