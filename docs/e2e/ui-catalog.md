# UI カタログ（開発用）

- 略号: `UC`
- 画面: `frontend/src/views/UiCatalogView.vue`
- テスト: `frontend/e2e/ui-catalog.spec.js`

業務画面ではなく、`components/ui/` の共通部品を実物で見比べるための開発用ページ（`/dev/ui-catalog`）。
サイドメニューには載せない。部品ごとの詳しい振る舞いは [docs/unit/](../unit/README.md) 側で担保するので、
ここでは「カタログが壊れずに開けること」だけを守る。

| ID | 前提 | 操作 | 期待結果 | 状態 |
|---|---|---|---|---|
| UC-01 | 既定モック | `/dev/ui-catalog` を開く | ヘッダに「UI カタログ」が出て、ボタン / 入力（boxed）/ 入力（underline）/ ファイル取込み / 通知とチップ / カードとダイアログ の各節が表示される | 実装済 |
| UC-02 | カタログを開いている | 「削除確認を開く」を押す | 「削除確認」のダイアログが開き、「キャンセル」を押すと閉じる | 実装済 |
