# views/BlockedDateListView（受注不可日マスタ一覧画面）

- 略号: `BDL`
- 対象: `src/views/BlockedDateListView.vue`
- テスト: `src/views/BlockedDateListView.spec.js`

| ID | 前提 | 操作 | 期待結果 | 状態 |
|---|---|---|---|---|
| BDL-01 | API 応答がまだ返っていない | クエリなしでマウントする | ローディング表示が出て、表は出ない | 実装済 |
| BDL-02 | 既定モック（フィクスチャ全件）、クエリなし | マウントして応答を待つ | 行が 1 ページ分出て、件数が「〈全件数〉 件」、ページャーが「〈全件数〉 件中 1–〈表示件数〉 件」 | 実装済 |
| BDL-03 | API が 500（`message` 付き）を返す | マウントして応答を待つ | エラー表示にその message と「再試行」ボタンが出て、表は出ない | 実装済 |
| BDL-04 | API が `items: []` を返す | マウントして応答を待つ | 空状態の表示が出て、表もページャーも出ない | 実装済 |
| BDL-05 | API が 500 を返す | マウントして応答を待つ | 検索フォームは消えず、条件を入れ直せる | 実装済 |
| BDL-06 | 既定モック、`?offset=〈表示件数〉` で開く | マウントして応答を待つ | 2 ページ目（残りの件数）が出て、ページャーが「〈全件数〉 件中 〈表示件数+1〉–〈全件数〉 件」 | 実装済 |
| BDL-07 | 既定モック、`?offset=7`（表示件数の倍数でない） | マウントして応答を待つ | 端数の位置から読み込まれ、先頭行がフィクスチャの 8 件目になる。ページャーの件数ラベルは現在ページ（1 ページ目）から導かれるため、見えている行とは一致しない（許容する仕様） | 実装済 |
| BDL-08 | 既定モック、クエリなしで表示済み | ページャーの「2」を click | URL クエリが `offset=〈表示件数〉` になり、表が 2 ページ目に入れ替わる | 実装済 |
| BDL-09 | 既定モック、クエリなしで表示済み | 日付 From / To を入力して検索を実行する | URL クエリに `date_from` / `date_to` が乗り、その期間の受注不可日だけが出る | 実装済 |
| BDL-10 | `?date_from=…&date_to=…` で絞り込み済み | 「クリア」を click | URL クエリが空になり、全件の 1 ページ目に戻る | 実装済 |
| BDL-11 | `?date_from=…&date_to=…` で開く | マウントする | 日付 From / To の入力欄に URL の値が入っている | 実装済 |
| BDL-12 | 初回応答は 0 件、2 回目は既定モック | 「再読み込み」を click | 表が 1 ページ目に更新され、URL クエリは変わらない | 実装済 |
| BDL-13 | ローディング / エラー / 空 / データあり の 4 状態 | それぞれの状態でマウントする | 説明バナーはどの状態でも表示され続ける | 実装済 |
| BDL-14 | 既定モック、クエリなし | マウントして応答を待つ | 表の見出しが「日付 / 理由」と見出しの無い操作列で、各行の 2 列の内容がフィクスチャの値と一致し、操作列に編集と削除のボタンが出る | 実装済 |
| BDL-15 | 既定モック、表示済み | 「新規追加」を click | 追加モーダルが開き、日付 / 理由の入力欄が空で出る（入力項目はこの 2 つだけ） | 実装済 |
| BDL-16 | 追加モーダルを開いた状態 | 未入力のまま「追加」を click | 「日付を入力してください。」「理由を入力してください。」が項目ごとに出て、モーダルは開いたまま。事前検証・登録のどちらの API も呼ばれず、一覧の件数も成功メッセージも変わらない | 実装済 |
| BDL-17 | 既定モック、一覧に無い日付と理由を入力済み | 「追加」を click | モーダルが閉じ、その日付を含む成功メッセージが出て、一覧の件数が 1 増える。URL クエリは変わらない | 実装済 |
| BDL-18 | 既定モック、すでに登録済みの日付と理由を入力済み | 「追加」を click | モーダルが開いたまま、事前検証の理由が専用の箇条書き領域に出る。項目直下のエラーには混ざらず、通信障害用の表示も出ない。成功メッセージは出ず件数も変わらない | 実装済 |
| BDL-19 | 事前検証が理由を 2 件返す | 入力して「追加」を click | 箇条書きの項目数が理由の件数と一致し、各行にその文言が出る | 実装済 |
| BDL-20 | 事前検証の API が 500（`message` 付き）を返す | 入力して「追加」を click | モーダルが開いたまま通信障害用の表示にその message が出る。事前検証の箇条書きは出ず、成功メッセージも出ない | 実装済 |
| BDL-21 | 事前検証で弾かれた直後 | 「キャンセル」で閉じ、もう一度「新規追加」を click | 箇条書きの理由が消えており、入力欄も空に戻っている | 実装済 |
| BDL-22 | 事前検証の応答が遅れて返る、入力済み | 「追加」を click し、応答前の状態を見る | 追加ボタンの文言が「追加中…」になり、追加 / キャンセルの両方が押せない（モーダルも閉じない） | 実装済 |
| BDL-23 | 既定モック、表示済み | 先頭行の「削除」を click | 削除確認モーダルが開き、その行の日付が本文に出る | 実装済 |
| BDL-24 | 削除確認モーダルを開いた状態 | 「キャンセル」を click | モーダルが閉じ、一覧の件数も行数も変わらない（API を呼ばない） | 実装済 |
| BDL-25 | 削除確認モーダルを開いた状態 | 「削除する」を click | モーダルが閉じ、その日付を含む成功メッセージが出て、一覧の件数が 1 減り、その行の削除ボタンが消える | 実装済 |
| BDL-26 | DELETE が 404（`message` 付き）を返す | 「削除する」を click | モーダルが開いたままその message が出て、成功メッセージは出ず件数も変わらない | 実装済 |
| BDL-27 | 絞り込みで最終ページが 1 件だけになる URL（`?date_from=…&date_to=…&offset=〈表示件数〉`）で開く | その 1 件を削除する | URL クエリから `offset` が消えて 1 ページ前に戻り、絞り込み条件は残る | 実装済 |
| BDL-28 | 既定モック、表示済み | 先頭行の「編集」を click | 編集モーダルが開き、日付 / 理由にその行の現在値が入る（追加モーダルは開かない） | 実装済 |
| BDL-29 | 編集モーダルを開いた状態 | 理由だけを変えて「更新」を click | モーダルが閉じ、その日付を含む成功メッセージが出る。一覧の件数は変わらず、その行の理由が新しい値になる | 実装済 |
| BDL-30 | 編集モーダルを開いた状態 | 日付を一覧に無い日付に変えて「更新」を click | モーダルが閉じ、成功メッセージが**新しい**日付を含む。一覧の件数は変わらず、新しい日付の行が出て元の日付の行は消える | 実装済 |
| BDL-31 | 編集モーダルを開いた状態 | 日付と理由を空にして「更新」を click | 「日付を入力してください。」「理由を入力してください。」が項目ごとに出て、モーダルは開いたまま。事前検証・更新のどちらの API も呼ばれず、一覧も成功メッセージも変わらない | 実装済 |
| BDL-32 | 更新の API が 409（`message` 付き）を返す（事前検証は通る） | 「更新」を click | モーダルが開いたまま通信障害用の表示にその message が出る。事前検証の箇条書きは出ず、成功メッセージも出ない。一覧の内容も変わらない | 実装済 |
| BDL-33 | 事前検証の応答が遅れて返る、編集モーダルを開いた状態 | 「更新」を click し、応答前の状態を見る | 更新ボタンの文言が「更新中…」になり、更新 / キャンセルの両方が押せない（モーダルも閉じない） | 実装済 |
| BDL-34 | 編集の事前検証で弾かれた直後 | 「キャンセル」で閉じ、別の行の「編集」を click | 箇条書きの理由が消えており、入力欄にはその行の現在値が入っている | 実装済 |
| BDL-35 | 編集の事前検証で弾かれた直後 | 「新規追加」を click | 追加モーダル側に編集のエラーは出ない。逆順（追加で弾かれた直後に「編集」を開く）でも編集モーダル側に追加のエラーは出ない | 実装済 |
| BDL-36 | 絞り込みで最終ページが 1 件だけになる URL（`?date_from=…&date_to=…&offset=〈表示件数〉`）で開く | その 1 件の日付を絞り込みの範囲外に変えて「更新」を click | URL クエリから `offset` が消えて 1 ページ前に戻り、絞り込み条件は残る | 実装済 |
