# views/CustomerListView（顧客マスタ一覧画面）

- 略号: `CLV`
- 対象: `src/views/CustomerListView.vue`
- テスト: `src/views/CustomerListView.spec.js`

実際の Pinia ストア + vue-router + MSW(node) を通して、
**4 状態の出し分け**と「ページ位置・検索条件は URL クエリが正」の単方向フローを守る。

実ブラウザでの URL 同期は E2E（`docs/e2e/customers.md`）が別に見る。こちらは E2E では安定して
観測できないもの（過渡状態・手で書き換えられた URL クエリ・応答の差し替え・
コードマスタが未取得の瞬間）を担保する。

この画面に固有の、取り違えやすい点を 4 つ固定する。

- **プルダウンの選択肢はコードマスタ由来。** この画面は `load` を呼ばない（`main.js` が起動時に
  1 回だけ読む）ので、コードマスタが未取得のままでも検索カードは描けなければならない（CLV-19）
- **`米国株評価額` / `評価損益` は見出しだけ。** 値の出所が未定でアプリ内モデルにも項目が無く、
  セルは常に `—`（CLV-04）。列を消すのも値を作るのも実装の変更になる
- **金額は単位を後置する**（`3,500,000 円` / `50,000.00 ドル`）。
  残高 0 は `0 円` で、`null`（値が無い）だけが `—`（CLV-07）
- **バッジは 2 種類ある。** 取引停止は取引規制の列、事故処理口座は口座区分の列に**併記**する
  （区分名を置き換えない）

この画面は**読むだけ**で、新規追加・編集・削除の導線を持たない（CLV-21）。
期待値はフィクスチャ（`src/mocks/fixtures/customers.js` / `codes.js`）と
`CUSTOMERS_PAGE_SIZE` から導く。

| ID | 前提 | 操作 | 期待結果 | 状態 |
|---|---|---|---|---|
| CLV-01 | 既定モック | マウント直後（応答前）を見る | 回転マークが出て、表も空状態も出ない。件数も出ない（確定前の値を新しい結果に見せない） | 実装済 |
| CLV-02 | 既定モック | マウントして応答を待つ | 件数がフィクスチャの全件数で、表の行が `CUSTOMERS_PAGE_SIZE` 件。1 行目の部店コード・部店名・扱者・口座番号・顧客名・カナがフィクスチャの先頭行と一致する | 実装済 |
| CLV-03 | 既定モック | 表の列見出しを読む | `部店` / `扱者` / `口座番号` / `顧客名` / `年齢` / `取引規制` / `投資方針` / `コンプラ` / `口座区分` / `個人／法人` / `円貨預り金` / `USD預り金` / `成長投資枠` / `米国株評価額` / `評価損益` の 15 列がこの順に並ぶ。操作列は無い | 実装済 |
| CLV-04 | 既定モック | 米国株評価額と評価損益のセルを読む | どの行も `—`（値の出所が未定のため見出しだけを置いている） | 実装済 |
| CLV-05 | 既定モック（ユーザー操作フラグ 1 と 0 の行を含む） | マウントして応答を待つ | フラグ 1 の行にだけ `is-user-modified` が付き、0 の行には付かない | 実装済 |
| CLV-06 | 既定モック | 金額 3 列を読む | 円貨・成長投資枠が `3,500,000 円`、USD 預り金が `50,000.00 ドル` の形（桁区切りと単位。USD は小数第 2 位まで） | 実装済 |
| CLV-07 | 外貨預り金が `null`・円貨預り金が `0` の行がある | マウントして応答を待つ | `null` の列は `—`、`0` の列は `0 円`（残高 0 と「値が無い」を同じに見せない） | 実装済 |
| CLV-08 | 年齢を持つ行と法人（年齢が空）の行がある | 年齢のセルを読む | 個人は `75歳` の形、法人は `—` | 実装済 |
| CLV-09 | 取引停止の行と通常の行がある | 取引規制のセルを読む | 停止中の行だけバッジ（`warning`）で区分名が出て、通常の行はバッジなしの区分名になる | 実装済 |
| CLV-10 | 事故処理口座の行がある | 口座区分のセルを読む | 区分名に加えて「事故」バッジが併記される（区分名を置き換えない）。通常の行にはバッジが無い | 実装済 |
| CLV-11 | API が 0 件を返す | マウントして応答を待つ | 「該当する顧客はありません。」が出て、表は描画されない | 実装済 |
| CLV-12 | API が 500 を返す | マウントして応答を待つ | エラーの理由と「再試行」ボタンが出て、表も空状態も出ない | 実装済 |
| CLV-13 | API が 500 を返した後、正常応答に戻る | 「再試行」を click | 表が描画され、エラー表示が消える | 実装済 |
| CLV-14 | 既定モック | ページ送りの「2」を click | URL に `offset` が乗り、表が 2 ページ目に入れ替わる | 実装済 |
| CLV-15 | 既定モック。コードマスタ取得済み | 部店と顧客名を入れて「検索」を click | URL に `branch_code` / `customer_name` が乗り、表が絞り込まれる。`offset` は付かない（1 ページ目に戻る） | 実装済 |
| CLV-16 | 既定モック。絞り込み済み | 「クリア」を click | URL クエリが空になり、全件に戻る | 実装済 |
| CLV-17 | `?branch_code=…&account_type=…` でマウント | 応答を待つ | 条件が入力欄に反映され、その条件で絞り込まれた表が出る（ブックマークとブラウザバックが効く） | 実装済 |
| CLV-18 | 既定モック。コードマスタ取得済み | 部店・扱者・取引規制・口座区分・個人／法人の選択肢を読む | いずれもコードマスタの内容と一致し、先頭に空値の既定選択肢（`-- 全部店 --` など）が入る | 実装済 |
| CLV-19 | コードマスタが未取得 | マウントして応答を待つ | 検索カードの各セレクトが既定選択肢だけで描かれ、画面は壊れない（この画面はコードマスタを読み込まない） | 実装済 |
| CLV-20 | API が 0 件 / 500 / 正常 のいずれか | マウントして応答を待つ | 説明バナーと検索カードはどの状態でも消えない（0 件やエラーのときこそ条件を直したい） | 実装済 |
| CLV-21 | 既定モック | ヘッダと表の操作を見る | ヘッダに「新規追加」が無い。行にも編集・削除のボタンが無い | 実装済 |
| CLV-22 | コードマスタを読み込み始めた直後（応答前） | 検索カードを見る | 回転マークが出て、入力欄と「検索」が無効になる。取得が終わると回転マークが消え、また入力できるようになる | 実装済 |
