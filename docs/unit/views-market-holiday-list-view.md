# views/MarketHolidayListView（海外休場日マスタ一覧画面）

- 略号: `MHL`
- 対象: `src/views/MarketHolidayListView.vue`
- テスト: `src/views/MarketHolidayListView.spec.js`

| ID | 前提 | 操作 | 期待結果 | 状態 |
|---|---|---|---|---|
| MHL-01 | API 応答がまだ返っていない | クエリなしでマウントする | ローディング表示が出て、表は出ない | 実装済 |
| MHL-02 | 既定モック（56 件）、クエリなし | マウントして応答を待つ | 行が 1 ページ分（50 件）出て、件数が「56 件」、ページャーが「56 件中 1–50 件」 | 実装済 |
| MHL-03 | API が 500（`message` 付き）を返す | マウントして応答を待つ | エラー表示にその message と「再試行」ボタンが出て、表は出ない | 実装済 |
| MHL-04 | API が `items: []` を返す | マウントして応答を待つ | 空状態の表示が出て、表もページャーも出ない | 実装済 |
| MHL-05 | API が 500 を返す | マウントして応答を待つ | 検索フォームは消えず、条件を入れ直せる | 実装済 |
| MHL-06 | 既定モック、`?offset=50` で開く | マウントして応答を待つ | 2 ページ目（残り 6 件）が出て、ページャーが「56 件中 51–56 件」 | 実装済 |
| MHL-07 | 既定モック、`?offset=7`（表示件数の倍数でない） | マウントして応答を待つ | 1 ページ目として扱われ、「56 件中 1–50 件」になる | 実装済 |
| MHL-08 | 既定モック、クエリなしで表示済み | ページャーの「2」を click | URL クエリが `offset=50` になり、表が 2 ページ目に入れ替わる | 実装済 |
| MHL-09 | 既定モック、クエリなしで表示済み | 日付 From / To を入力して検索を実行する | URL クエリに `date_from` / `date_to` が乗り、その期間の休場日だけが出る | 実装済 |
| MHL-10 | `?date_from=…&date_to=…` で絞り込み済み | 「クリア」を click | URL クエリが空になり、全 56 件の 1 ページ目に戻る | 実装済 |
| MHL-11 | `?date_from=…&date_to=…` で開く | マウントする | 日付 From / To の入力欄に URL の値が入っている | 実装済 |
| MHL-12 | 初回応答は 0 件、2 回目は既定モック | 「再読み込み」を click | 表が 1 ページ目に更新され、URL クエリは変わらない | 実装済 |
| MHL-13 | 既定モック、表示済み | 「新規追加」を click | 追加モーダルが開き、日付 / 休場理由の入力欄が空で出る | 実装済 |
| MHL-14 | 追加モーダルを開いた状態 | 未入力のまま「追加」を click | 項目ごとのエラー文言が出て、モーダルは開いたまま。一覧の件数は変わらない（API を呼ばない） | 実装済 |
| MHL-15 | 既定モック、一覧に無い日付と理由を入力済み | 「追加」を click | モーダルが閉じ、成功メッセージが出て、一覧の件数が 1 増える | 実装済 |
| MHL-16 | POST が 409（`message` 付き）を返す | 入力して「追加」を click | モーダルが開いたままその message が出て、成功メッセージは出ない | 実装済 |
| MHL-17 | 追加が 409 で失敗した直後 | 「キャンセル」で閉じ、もう一度「新規追加」を click | 前回のエラー文言が消えており、入力欄も空に戻っている | 実装済 |
| MHL-18 | 既定モック、表示済み | 先頭行の「削除」を click | 削除確認モーダルが開き、その行の日付が本文に出る | 実装済 |
| MHL-19 | 削除確認モーダルを開いた状態 | 「キャンセル」を click | モーダルが閉じ、一覧の件数は変わらない（API を呼ばない） | 実装済 |
| MHL-20 | 削除確認モーダルを開いた状態 | 「削除する」を click | モーダルが閉じ、その日付を含む成功メッセージが出て、一覧の件数が 1 減る | 実装済 |
| MHL-21 | DELETE が 404（`message` 付き）を返す | 「削除する」を click | モーダルが開いたままその message が出て、成功メッセージは出ず件数も変わらない | 実装済 |
| MHL-22 | 絞り込みで最終ページが 1 件だけになる URL（`?date_from=…&date_to=…&offset=50`）で開く | その 1 件を削除する | URL クエリから `offset` が消えて 1 ページ前に戻り、絞り込み条件は残る | 実装済 |
| MHL-23 | 既定モック、クエリなし | マウントして応答を待つ | 表に「休場区分」の見出しが出て、各行にコードではなく表示名（終日休場 / 短縮取引）が出る | 実装済 |
| MHL-24 | 既定モック、表示済み | 休場区分セレクトで「短縮取引」を選び検索を実行する | URL クエリに `holiday_type=1` が乗り、短縮取引の行だけに絞られて件数もその値になる | 実装済 |
| MHL-25 | `?holiday_type=1` で開く | マウントして応答を待つ | セレクトの値が `1` で、一覧も短縮取引の行だけになる | 実装済 |
| MHL-26 | `?holiday_type=9`（選択肢に無い値）で開く | マウントして応答を待つ | 条件なしとして扱われ、セレクトは空、一覧は全件の 1 ページ目になる | 実装済 |
| MHL-27 | 既定モック、表示済み | 「新規追加」を click し、休場区分を「短縮取引」に変えて「キャンセル」→ もう一度「新規追加」 | 開いた直後の休場区分は既定値（終日休場）で、変更を持ち込まずリセットされている | 実装済 |
| MHL-28 | 既定モック、一覧に無い日付と理由を入力、休場区分に「短縮取引」を選択 | 「追加」を click | モーダルが閉じて成功メッセージが出て件数が 1 増え、短縮取引だけに絞ると増えた行が含まれる | 実装済 |
