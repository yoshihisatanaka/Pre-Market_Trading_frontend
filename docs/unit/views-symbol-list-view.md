# views/SymbolListView（銘柄マスタ一覧画面）

- 略号: `STV`
- 対象: `src/views/SymbolListView.vue`
- テスト: `src/views/SymbolListView.spec.js`

実際の Pinia ストア + vue-router + MSW(node) を通して、
**4 状態の出し分け**と「ページ位置・検索条件は URL クエリが正」の単方向フローを守る。

実ブラウザでの URL 同期は銘柄マスタの E2E シナリオ（`docs/e2e/` 配下・別途）が見る。
こちらは E2E では安定して観測できないもの（過渡状態・手で書き換えられた URL クエリ・
応答の差し替え）を担保する。分担は次のとおり。

| 論点 | E2E | 単体 | 理由 |
|---|---|---|---|
| ページ送り・検索の URL 同期 | E2E 側で見る | STV-12 / 13 / 14 | 実ブラウザの履歴まで見るのは E2E、条件の受け渡しは単体 |
| 未知の `regulation` | — | STV-15 | 画面の導線からは起こらない（手で URL を書いたときだけ） |
| 区分名が欠けた応答 | — | STV-07 | 応答の差し替えが要る。実 API は常に付けて返す |
| 表示件数の倍数でない `offset` | — | STV-11 | 同上。画面の導線からは起こらない |
| 相場が未取得（`null`）の行 | — | STV-05 | フィクスチャ依存の行を狙い撃つ |

検証するのは**表示名**であって、取引可否セルの色ではない。`規制情報` のコード値は
仮置き（[utils-symbol-types.md](utils-symbol-types.md)）で、差し替わると色の条件も変わるため。

この画面は**一覧と新規追加**を持つ（編集・削除はまだ無い。STV-17）。
追加についての分担は次のとおり。手本は
[views-corporate-action-list-view.md](views-corporate-action-list-view.md)（CAV-17〜23）。

| 論点 | E2E | 単体 | 理由 |
|---|---|---|---|
| 追加できること・成功の見えかた | SM-15 / 20 | STV-20 | 実ブラウザで通ることは E2E、条件を保った読み直しは単体 |
| 必須 3 項目の未入力 | SM-14 | STV-19 | 文言と出る位置は単体のほうが細かく見られる |
| 事前検証の不合格と通信障害の出し分け | SM-16 / 17 | STV-21 / 22 | 応答の差し替えが要る |
| 登録中はキャンセルもできない | — | STV-23 | 過渡状態は E2E では安定して観測できない |

画面が自分で弾くのは**必須 3 項目（銘柄コード・ティッカーコード・銘柄名（日本語））が
空でないこと**だけ。銘柄コードの重複・文字数超過・コードの実在はサーバの事前検証が見る
（重複の出しかたは STV-21）。区分 3 つは未選択を作らず実 API の既定（`'0'`）から始めるので、
選択の必須検証は無い（STV-25）。

**`市場名` / `前日出来高` / `Pre区分` はフォームに無い。** 画面から作れない項目なので送らない
（`src/api/symbols.js` の JSDoc に理由がある）。入力欄の並びと `data-testid` は
`src/components/symbol/SymbolFormFields.vue` が持ち、追加と編集でずれないように部品化してある
（`src/components/masters/` と同じ扱いで、その部品単独のシナリオ文書は持たない）。

期待値はフィクスチャ（`src/mocks/fixtures/symbols.js`）・`SYMBOLS_PAGE_SIZE`・
`src/utils/format.js` の整形関数から導く。

| ID | 前提 | 操作 | 期待結果 | 状態 |
|---|---|---|---|---|
| STV-01 | 既定モック | マウント直後（応答前）を見る | 回転マークが出て、表も空状態も出ない | 実装済 |
| STV-02 | 既定モック | マウントして応答を待つ | 件数がフィクスチャの全件数で、表の行が `SYMBOLS_PAGE_SIZE` 件。1 行目の銘柄コード・ティッカーコード・銘柄名（英語・日本語）がフィクスチャの先頭行と一致する | 実装済 |
| STV-03 | 既定モック | 表の列見出しを読む | `銘柄コード` / `ティッカーコード` / `銘柄名（英語）` / `銘柄名（日本語）` / `前日終値` / `前日出来高` / `5日平均出来高` / `取引可否` / `預託先区分` / `VWAP対象区分` / `備考` の 11 列がこの順に並ぶ。操作列は無い | 実装済 |
| STV-04 | 既定モック（ユーザー操作フラグ 1 と 0 の行を含む） | マウントして応答を待つ | フラグ 1 の行にだけ `is-user-modified` が付き、0 の行には付かない | 実装済 |
| STV-05 | 相場の 3 項目が未取得（`null`）の行がある | マウントして応答を待つ | その行の前日終値・前日出来高・5日平均出来高がいずれも `—` になる（空欄にしない） | 実装済 |
| STV-06 | 既定モック | 1 行目の相場のセルを読む | 前日終値が `227.16 ドル` の形（通貨記号を使わない）、出来高 2 列が 3 桁区切りの整数で出る | 実装済 |
| STV-07 | API が区分名（`規制情報名` / `注文ルート名` / `VWAP対象区分名`）を持たない行を返す | マウントして応答を待つ | 3 つのセルにコードから引いた表示名が出る（コードの数字をそのまま出さない） | 実装済 |
| STV-08 | API が 0 件を返す | マウントして応答を待つ | 「該当する銘柄はありません。」が出て、表は描画されない | 実装済 |
| STV-09 | API が 500 を返す | マウントして応答を待つ | エラーの理由と「再試行」ボタンが出て、表も空状態も出ない | 実装済 |
| STV-10 | API が 500 を返した後、正常応答に戻る | 「再試行」を click | 表が描画され、エラー表示が消える | 実装済 |
| STV-11 | 既定モック。`?offset=7`（表示件数の倍数でない） | マウントして応答を待つ | 丸めずその位置から `SYMBOLS_PAGE_SIZE` 件が出る | 実装済 |
| STV-12 | 既定モック | ページ送りの「2」を click | URL に `offset` が乗り、表が 2 ページ目に入れ替わる | 実装済 |
| STV-13 | 既定モック | 銘柄コード・取引可否・預託先区分・VWAP対象区分を入れて「検索」を click | URL に `symbol_code` / `regulation` / `order_route` / `vwap_target` が乗り、表が絞り込まれる。`offset` は付かない（1 ページ目に戻る） | 実装済 |
| STV-14 | 既定モック。絞り込み済み | 「クリア」を click | URL クエリが空になり、全件に戻る | 実装済 |
| STV-15 | `?regulation=9`（実在しないコード） | マウントして応答を待つ | 条件なしとして捨てられ、全件が出る。セレクトは「-- すべて --」のまま | 実装済 |
| STV-16 | API が 0 件 / 500 / 正常 のいずれか | マウントして応答を待つ | 説明バナーと検索カードはどの状態でも消えない（0 件やエラーのときこそ条件を直したい） | 実装済 |
| STV-17 | 既定モック | ヘッダと表の操作を見る | ヘッダに「再読み込み」と「新規追加」がある。行には編集・削除のボタンが無い（まだ実装していない操作を見せない） | 実装済 |
| STV-18 | 既定モック | 「新規追加」を click | モーダルが開き、10 項目の入力欄が出る。文字と数値の欄は空 | 実装済 |
| STV-19 | 既定モック。追加モーダルを開いた | 何も入力せず「追加」を click | 銘柄コード・ティッカーコード・銘柄名（日本語）の直下に必須の理由が出て、モーダルは開いたまま。API へは送らない | 実装済 |
| STV-20 | 既定モック | 「新規追加」を click し、必須 3 項目を入れて「追加」を click | モーダルが閉じ、追加した銘柄コードを含む成功メッセージが出る。件数が 1 増える | 実装済 |
| STV-21 | 事前検証が不合格を返す（既にある銘柄コード） | 追加モーダルで「追加」を click | モーダル内に理由が箇条書きで出て、モーダルは開いたまま。件数は変わらず、項目直下のエラーには混ざらない | 実装済 |
| STV-22 | `POST /api/masters/symbols` が 500 を返す | 追加モーダルで「追加」を click | モーダル内に通信・サーバ障害の理由が 1 行出て、モーダルは開いたまま。事前検証の箇条書きは出ない | 実装済 |
| STV-23 | `POST /api/masters/symbols` の応答が遅い | 「追加」を click した直後を見る | ボタンが「追加中…」になり、送信もキャンセルも押せない（結果の行き先が無くなるため） | 実装済 |
| STV-24 | 既定モック。追加が失敗した後 | モーダルを閉じて開き直す | 前回の失敗理由と入力値が残らない（空のフォームで始まる） | 実装済 |
| STV-25 | 既定モック | 「新規追加」を click し、区分 3 つの選択値を読む | 取引可否 `0` / 預託先区分 `0` / VWAP対象区分 `0` が選ばれている（未選択を作らない。実 API の既定と同じ） | 実装済 |
